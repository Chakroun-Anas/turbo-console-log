# Ignore everything by default
**/*

# Include only the files specified in package.json "files" array
!out/extension.js
!images/icon.png
!README.md
!CHANGELOG.md
!LICENSE.txt
!package.json

# Ensure parent directories are not ignored (required for nested paths)
!out/
!images/
