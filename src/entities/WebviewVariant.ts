export interface WebviewVariantResponse {
  variant: string;
  version: string;
  title: string;
  htmlContent: string;
}

export interface WebviewVariantConfig {
  title: string;
  htmlContent: string;
}
