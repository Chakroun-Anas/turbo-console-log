export default {
  fileExtension: '.ts',
  name: 'array destructuring with binary expression',
  lines: ['const [first, second] = getValue() || getDefault();'],
  selectionLine: 0,
  variableName: 'first',
};
