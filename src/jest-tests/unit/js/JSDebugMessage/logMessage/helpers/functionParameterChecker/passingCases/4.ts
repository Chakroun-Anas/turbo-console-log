export default {
  name: 'nested object destructured parameter',
  lines: [
    'const handleInit = ({ config: { apiKey, region } }) => {',
    '  console.log(apiKey);',
    '};',
  ],
  selectionLine: 0,
  variableName: 'apiKey',
};
