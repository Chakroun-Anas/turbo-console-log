/**
 * Centralized enum for all global state keys used throughout the extension
 * This prevents typos and provides better IntelliSense support
 */
export enum GlobalStateKeys {
  DYNAMIC_FREEMIUM_PANEL_CONTENT = 'dynamicFreemiumPanelContent',
  DYNAMIC_FREEMIUM_PANEL_LAST_FETCH = 'dynamicFreemiumPanelLastFetch',
  DYNAMIC_FREEMIUM_PANEL_LAST_ACCESS = 'dynamicFreemiumPanelLastAccess',
}
